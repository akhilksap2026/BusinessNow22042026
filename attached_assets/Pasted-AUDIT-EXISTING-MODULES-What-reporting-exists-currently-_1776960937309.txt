AUDIT EXISTING MODULES:
- What reporting exists currently?
- Are there any analytics, charts, or export features?
- Is there a data warehouse or BI connection?

GAP MAPPING:
→ Can you generate reports on project performance?
→ Can you compare planned vs actual timelines?
→ Can you identify scope creep (non-template work)?

INTEGRATION STEPS:
1. PROJECT PERFORMANCE REPORT (reuse existing project list):
   - ADD new view/route: `/reports/project-performance`
   - Reuse existing project query, add computed columns:
     - on_time_rate: tasks completed by due_date / total tasks
     - csat_average: from Prompt 4
     - template_used: from Prompt 2 `from_template` tracking
   - ADD filter sidebar: date range, status, owner, customer, template
   - ADD CSV export (reuse existing export utility)

2. OPERATIONS INSIGHTS REPORT (new):
   - Group projects by template
   - Calculate on-time % per template (bar chart — reuse existing chart library)
   - List template durations: planned (from template) vs actual (from project dates)
   - ADD "Non-Template Tasks" column: COUNT tasks where from_template = false
   - This requires no new tables — queries existing tasks with existing flag

3. CSAT REPORT (new):
   - Query `csat_surveys` table (from Prompt 4)
   - Aggregate by: date (month), project, customer, template
   - Trend line: average rating over time
   - Reuse existing chart component for line graph

4. INTERVAL IQ REPORT (new):
   - CREATE `key_events` table: id, project_id, name, event_date, event_type
   - CREATE `intervals` table: id, project_id, name, start_event_id, end_event_id, benchmark_days
   - BACKFILL: populate key_events from existing task/phase completion dates
   - Calculate actual_days = end_event_date - start_event_date
   - Highlight where actual_days > benchmark_days
   - Visual: timeline or bar chart showing actual vs benchmark

VERIFICATION:
- [ ] Project Performance report shows correct on-time % for 10 projects
- [ ] Operations Insights shows template comparison with non-template task counts
- [ ] CSAT report trend line matches submitted ratings over time
- [ ] Interval IQ flags project where "Kickoff to Go-Live" took 45 days vs 30-day benchmark