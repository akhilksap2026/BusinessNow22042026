AUDIT EXISTING MODULES:
- Do you have project templates already?
- Can templates define phases and tasks?
- Is there any concept of "template-level" resource planning?

GAP MAPPING:
→ Can templates include placeholder allocations?
→ Are template allocations using relative days (Day 1, Day 2) instead of calendar dates?
→ When project created from template, do allocations carry over with correct dates?
→ Can auto-allocate be configured at template level?

INTEGRATION STEPS:
1. ADD "Allocations" tab to existing template detail/edit view
   - Reuse same timeline component from Prompt 3
   - X-axis: relative days (Day 1, Day 2, ... Day N) instead of calendar dates

2. CREATE `template_allocations` table (NEW):
   - id, template_id, placeholder_id (or resource_id for named defaults),
     relative_start_day INT, relative_end_day INT,
     hours_per_day DECIMAL(4,2), allocation_method, method_value,
     phase_id (nullable, for alignment)

3. ADD template allocation actions:
   - Drag-create bars on relative-day timeline
   - Same edit panel: hours, method, hard/soft
   - Add placeholder from template (reuse existing placeholder system)
   - Enable/disable auto_allocate flag for template

4. ADD project creation hook:
   - WHEN project created from template:
     a. Copy phases/tasks (existing logic)
     b. FOR EACH template_allocation:
        - Calculate absolute dates: project.start_date + relative_start_day
        - Create allocation record with calculated dates
        - If placeholder: create placeholder allocation (unfilled)
        - If named resource: create resource allocation (if still active)
     c. Copy auto_allocate setting to project

5. ADD total allocation summary to template:
   - Bottom of allocations tab: aggregate hours for entire template
   - Show per-role breakdown

VERIFICATION:
- [ ] Template with 3 placeholders, allocations Day 1-10 and Day 11-20
- [ ] Create project starting 2026-05-01 → allocations at 2026-05-01 to 2026-05-10 and 2026-05-11 to 2026-05-20
- [ ] Auto-allocate enabled in template → new project has auto_allocate=true
- [ ] Named resource in template (e.g., "Project Manager") → carries over if still active
- [ ] **GAP CAPTURE**: Document relative day calculation edge cases, inactive resource handling