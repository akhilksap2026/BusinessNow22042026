FIX 1 — Internal account type
File: lib/db/src/schema/accounts.ts
Add: accountType: text("account_type").notNull().default("client"),
Run: cd lib/db && npm run push

Seed KSAP: check if account named "KSAP" exists; if not, INSERT:
  { name:"KSAP", domain:"ksap.internal", tier:"Internal", region:"CA",
    status:"Active", contractValue:"0", accountType:"internal" }

API (accounts.ts): include accountType in GET/POST/PATCH.
Add optional ?type=internal query param to GET /accounts.
OpenAPI: add accountType to Account, CreateAccountBody, UpdateAccountBody.
Regenerate Orval hooks.

Frontend (projects.tsx New Project dialog): show "Internal" badge next to
internal accounts in the Account Select. No filtering — show all accounts.

FIX 2 — New Allocation: project dropdown
Find the Allocation creation form (resources.tsx or allocation dialog).
Replace the Project text input with a searchable combobox:
  useListProjects({ status: "Active" }) — filter client-side if no status param support.
  Display: project name. Value: project.id.
The Team & Allocations tab within a specific project must stay pre-filled — no change.

Add info Tooltip on Team & Allocations tab header:
  "Allocations define who is scheduled on this project and for how many hours
   per week. Time entries capture actual hours worked."

FIX 3 — Dashboard period filter
File: artifacts/api-server/src/routes/dashboard.ts
Accept ?period=week|month|quarter|ytd. Compute date range:
  week: currentWeek Mon→Sun, month: currentMonth, quarter: currentQuarter, ytd: Jan1→today.
Filter billableHoursInPeriod and totalRevenue (invoice issueDate in range).
outstandingInvoices: not date-filtered. Default period: "month".

Frontend (dashboard.tsx): un-disable "This Week", "Quarter", "YTD" options.
Pass selected period as query param to the summary hook.
Update KPI card captions to reflect selected period.

FIX 4 — Project description nudge
Below the Description textarea in New/Edit Project dialog:
  Add helper text: "A clear description helps your team understand project scope and goals."
  On Save with empty Description: show amber inline warning (not blocking):
  "Tip: Adding a description helps your team stay aligned."
  Do NOT make Description required (no .min(1) in zod schema).