AUDIT EXISTING MODULES:
- How are team members currently linked to projects? (membership table? assignment?)
- Is there any concept of "allocated hours" or "time commitment" per project?
- Do projects have start/end dates that could anchor allocations?
- Is there any "role-based" planning before specific people are assigned?

GAP MAPPING:
→ Can you assign a team member to a project with specific hours per day?
→ Can you plan with "role slots" before knowing who will fill them?
→ Are allocations time-bound (start date, end date) or just binary (assigned/not)?
→ Can allocations be tentative vs confirmed?

INTEGRATION STEPS:
1. CREATE `placeholders` table (NEW):
   - id, name, role_id, created_by, is_default BOOLEAN DEFAULT false,
     account_id
   - Default placeholders (system-defined) have is_default=true, name locked

2. CREATE `allocations` table (NEW — core entity):
   - id, project_id, 
     resource_id (FK to users, NULLABLE),
     placeholder_id (FK to placeholders, NULLABLE),
     start_date DATE, end_date DATE,
     hours_per_day DECIMAL(4,2),
     total_hours DECIMAL(8,2),
     allocation_type ENUM('hard','soft') DEFAULT 'hard',
     allocation_method ENUM('total_hours','hours_per_day','hours_per_week','percentage_capacity'),
     method_value DECIMAL(8,2),  -- stores the input value
     created_by, created_at, updated_at
   - CONSTRAINT: exactly one of resource_id or placeholder_id must be set
   - CONSTRAINT: end_date >= start_date

3. EXTEND existing project membership/assignment logic:
   - WHEN team member added to project: optionally create allocation record
   - WHEN team member removed from project: delete all their allocations for that project
   - ADD confirmation dialog before removal

4. ADD auto-calculation triggers:
   - IF allocation_method = 'total_hours':
     hours_per_day = total_hours / working_days_in_range
   - IF allocation_method = 'hours_per_day':
     total_hours = hours_per_day * working_days_in_range
   - IF allocation_method = 'hours_per_week':
     hours_per_day = hours_per_week / 5, total_hours = hours_per_day * working_days
   - IF allocation_method = 'percentage_capacity':
     hours_per_day = (percentage / 100) * (weekly_capacity / 5)
     -- NOTE: does NOT deduct existing allocations or time-offs

5. ADD `auto_allocate` BOOLEAN to existing projects table (DEFAULT false)
   - IF true: WHEN task assignee changes, auto-create matching allocation
   - Allocation dates = task start_date to task end_date
   - Allocation hours = task estimated_hours / working_days_in_range

VERIFICATION:
- [ ] Create allocation with resource_id → team member shows on project timeline
- [ ] Create allocation with placeholder_id → unfilled slot visible, "Find Team Member" available
- [ ] Auto-allocate project: assign task to team member → allocation auto-created
- [ ] Remove team member from project → all their allocations deleted with confirmation
- [ ] Percentage capacity allocation: 50% on 40hr/week person = 4hrs/day, ignores other allocations
- [ ] **GAP CAPTURE**: Document collision with existing assignment logic