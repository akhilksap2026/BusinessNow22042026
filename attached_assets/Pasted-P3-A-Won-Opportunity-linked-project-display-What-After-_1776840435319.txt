P3-A · Won Opportunity — linked project display

What: After "Create Project from Opportunity" succeeds, the opportunity detail still shows no project link
Fix: Store linkedProjectId on the opportunity record. The "Create Project" action in the opportunity detail already redirects — also PATCH the opportunity with the new project ID. In the opportunity detail sheet, show a "Linked Project → [name]" card with a navigation link
Files: opportunities.tsx, api-server/src/routes/opportunities.ts, DB schema migration
Effort: ~3 hrs
P3-B · Projects list — Archive from list page

What: Archiving a project requires navigating to Admin → Archived Projects. The project list has no archive action
Fix: Add Archive to the project row ⋮ dropdown (Admin/PM only). Calls DELETE /api/projects/:id (soft-delete). Move "Archived Projects" recovery out of Admin into a toggle/tab on the Projects page itself so it's discoverable
Files: projects.tsx, project-detail.tsx
Effort: ~2 hrs
P3-C · Session context — replace hardcoded CURRENT_USER_ID

What: CURRENT_USER_ID = 1 is hardcoded in time.tsx and task-detail-sheet.tsx. All time entries, comments, and approvals are always attributed to user 1 regardless of who is "logged in"
Fix: Create a useCurrentUser() context hook backed by a CurrentUserContext provider at the app root. Initialise from a GET /api/me endpoint (returns user 1 for now, but swappable with real auth). Replace all CURRENT_USER_ID = 1 references with const { id } = useCurrentUser(). The sidebar user chip reads from this context (name, initials, role) instead of hardcoded strings
Files: New src/contexts/current-user.tsx, layout.tsx, time.tsx, task-detail-sheet.tsx, new GET /api/me route
Effort: ~4 hrs
P3-D · Sidebar user chip — role switcher + logout

What: No role-switching capability; dual-role model has no UI surface. Depends on P3-C
Fix: The user chip dropdown (from P1-A) gains a "Switch Role" submenu showing the user's configured roles. Selecting one updates the CurrentUserContext.activeRole, which gates features (e.g., approval queue only appears when active role is PM or Admin). Admin → Users gains a secondary tab: User Configuration — where per-user secondary roles are assigned (a multi-select of available roles per user)
Files: layout.tsx, admin.tsx, current-user.tsx, api-server/src/routes/users.ts
Effort: ~6 hrs
