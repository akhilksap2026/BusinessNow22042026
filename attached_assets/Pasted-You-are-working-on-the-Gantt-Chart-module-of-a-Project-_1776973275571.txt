You are working on the Gantt Chart module of a Project Management Portal.

STEP 1 — AUDIT CURRENT STATE:
Inspect the existing Gantt chart. Identify:
- Current timeline scale (fixed or flexible)
- Whether task hierarchy (levels) is displayed
- Whether dependencies between tasks exist
- Current screen width utilization
- Whether horizontal scrolling exists and if the left pane is frozen
- Whether milestones are shown and how

STEP 2 — IMPLEMENT CHANGES:

2a. LAYOUT:
- Force the Gantt chart to stretch to 100% of the available viewport 
  width. Remove any fixed max-width containers.
- Implement a two-pane layout: LEFT pane (task names, fixed/frozen) 
  and RIGHT pane (timeline bars, horizontally scrollable).
- Left pane must remain stationary during horizontal scroll of the 
  right pane.

2b. TASK HIERARCHY & FILTERS:
- Render tasks in the Gantt respecting the nested hierarchy from 
  the task module (indented, with expand/collapse [+/-] icons).
- Add a filter control above the chart: "Show Levels: [1] [1-2] 
  [1-3] [All]" — clicking a level hides all tasks below that depth.

2c. TIMELINE SCALES:
- Add a scale selector: Day | Week | Month | Quarter
- Default to Month. Switching scale re-renders the timeline header 
  and adjusts bar widths proportionally.

2d. TASK BARS:
- Display the percentage complete as a numeric value (e.g. "45%") 
  rendered inside the filled portion of each task bar.
- Bar fill should reflect completion visually (partial fill left to right).

2e. DEPENDENCIES:
- Implement drag-and-drop dependency creation: user drags from the 
  right edge of one task bar to the left edge of another to create 
  a finish-to-start dependency.
- Render dependencies as arrows/lines connecting the tasks.
- On clicking a dependency line, show an edit popover allowing the 
  user to add a ± day buffer (lag/lead time).

2f. MILESTONES:
- Add a "Show Milestones" toggle above the chart.
- Milestones are rendered as diamond shapes on the timeline.
- Color code by type: Payment (green), Deliverable (blue), 
  Internal (orange).
- Add a color legend beneath or beside the toggle.
- On hover, show a tooltip with milestone name, date, and type.

STEP 3 — REMOVE REDUNDANT ELEMENTS:
- Remove any separate "Timeline" view that previously existed as a 
  simple date range bar — it is now fully replaced by this Gantt.
- Remove fixed month-only timeline header — replaced by the scale selector.

STEP 4 — VALIDATION:
After changes, confirm:
[ ] Gantt stretches to full screen width with no empty right margin
[ ] Left task pane stays frozen during horizontal scroll
[ ] Level filter correctly hides/shows tasks by depth
[ ] Day/Week/Month/Quarter scale switching works correctly
[ ] % complete number is visible inside task bars
[ ] Dragging between task bars creates a visible dependency arrow
[ ] ± day buffer can be set on a dependency link
[ ] Milestones appear as color-coded diamonds with correct legend
[ ] Show/hide milestones toggle works