In my existing Replit web application, replace the previous rule-based time-logging helper with a **smart, conversational AI-powered Time Tracking Assistant chatbot**.

**IMPORTANT: Do this in two clear phases**

**PHASE 1 — Deep Project Analysis**
First, thoroughly explore and analyze my entire current project. Understand:
- The exact tech stack (frontend framework, backend language/framework, database if any)
- All existing files and folders related to authentication, projects, tasks, time logging, users, and reports
- Current data models / schemas for Projects, Tasks, TimeLogs / TimeEntries, and User
- All existing API routes or backend functions, especially anything for:
  • Getting user’s projects
  • Getting tasks under a project
  • Creating / updating time log entries
  • Fetching time logs, summaries, or reports
  • Any other time-tracking related features
Summarize your understanding of my current time-tracking functionality before moving to Phase 2.

**PHASE 2 — Build the AI Chatbot**
Create a full AI-powered intelligent chatbot that acts as my personal Time Tracking Assistant with these capabilities:

1. **Natural & Flexible Conversation**
   - Understand free-form messages like:
     - “Log 6 hours on Website Redesign for the hero section”
     - “What’s my current status on all projects?”
     - “Show me my time logged this week”
     - “Help me log time for yesterday”
     - “Am I on track this month?”
     - “What tasks are still pending on Project Alpha?”

2. **Deep App Knowledge & Real-Time Data**
   - Always knows my current projects, allocated tasks, logged hours vs required hours, pending work, etc. by calling existing APIs/database.

3. **Full Time Tracking Journey Support**
   - Give smart insights and summaries (weekly/monthly totals, most active projects, consistency streaks)
   - Proactive warnings (“You’re about to exceed allocated hours on Task X”)
   - Gentle reminders and suggestions (“You haven’t logged time in 2 days — want to catch up?”)
   - Progress tracking toward project completion
   - Support single-day logging, multi-day logging, or any custom request

**Technical Implementation Requirements:**
- Create a modern floating chat widget (bottom-right button that opens a clean, professional chat window). Use Tailwind CSS if already in the project, otherwise nice custom CSS.
- Add a new secure backend endpoint (e.g. POST /api/ai-chat) that handles the conversation.
- Use a fast LLM (I prefer Groq with llama-3.3-70b or mixtral for speed and cost — create .env support for GROQ_API_KEY). If Groq is not suitable, use OpenAI GPT-4o-mini as fallback.
- Implement tool/function calling so the AI can:
  • fetch_current_projects()
  • fetch_tasks(project_id)
  • get_time_logs(date_range or project_id)
  • log_time_entries(data)
  • get_time_summary()
  • get_pending_tasks()
- Maintain conversation history/memory for the session.
- Use a strong system prompt that turns the LLM into a professional, friendly, proactive Time Tracking Assistant.

After completing the analysis in Phase 1, show me a short summary of what you understood about my app, then generate all the new code/files and integrate the chatbot cleanly into my existing project.

Make the code well-commented and modular so I can easily customize the personality or add more tools later. Make the UI mobile-friendly and professional.