AUDIT EXISTING MODULES:
- List all user/member tables and their current fields
- Identify: Do you have role-based access control? Department/team fields? 
- Is there any concept of "capacity" (hours per day/week) or "availability"?
- Do you track skills, proficiency levels, or certifications anywhere?
- Is there time-off/leave management already?

GAP MAPPING:
For each team member entity:
→ Does it have configurable weekly capacity (default 40 hrs/week)?
→ Does it have a skills profile with proficiency levels?
→ Does it have region/department/role attributes usable for filtering?
→ Is there a distinction between internal employees and client contacts?

INTEGRATION STEPS:
1. EXTEND existing user/member table:
   ADD `weekly_capacity_hours` DECIMAL(5,2) DEFAULT 40
   ADD `region` VARCHAR(50)
   ADD `department` VARCHAR(50)
   ADD `is_internal` BOOLEAN DEFAULT true  -- distinguishes from client contacts
   ADD `active_status` ENUM('active','inactive','on_leave') DEFAULT 'active'
   - Migration: set existing internal users to is_internal=true, clients to false

2. CREATE `skills` table (NEW — if not exists):
   - id, name, type (Yes-No/Number/Level/Single-Choice/Multiple-Choice), 
     section, associated_roles (JSON), description, account_id

3. CREATE `team_member_skills` junction table (NEW):
   - team_member_id, skill_id, proficiency_value (type-dependent: 
     boolean, number, enum level, string)
   - Composite primary key (team_member_id, skill_id)

4. CREATE `time_offs` table (NEW — if no leave system exists):
   - id, team_member_id, start_date, end_date, type (PTO/Sick/Other),
     status (Pending/Approved/Rejected), approved_by, approved_at
   - If leave system exists: ADD `status` tracking and integration hooks

5. CREATE `holidays` table (NEW):
   - id, name, date, applies_to (JSON: all, specific_teams, specific_users),
     account_id, region

6. ADD capacity calculation service/method:
   - Input: team_member_id, date_range
   - Formula: (weekly_capacity / 5) * working_days_in_range 
              - approved_time_off_hours - holiday_hours
   - Cache per-day capacity for performance

VERIFICATION:
- [ ] Existing user profiles show new capacity field (editable by Admin)
- [ ] Admin can add skills to team members via existing profile UI
- [ ] Approved time-off reduces calculated capacity correctly
- [ ] Holiday on team member's region reduces capacity by 0.2 FTE per day
- [ ] Client contacts (is_internal=false) excluded from resource pool
- [ ] **GAP CAPTURE**: Document any existing user types that don't map cleanly